# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: ID
        searchable: true

    url:
        label: URL

    parameters_lists:
        label: Parameters Lists

    use_internal_url:
        label: Use Internal URL

    internal_url:
        label: Internal URL